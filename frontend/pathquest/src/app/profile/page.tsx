/**
 * Profile page - renders the Profile tab (your data).
 * The actual content is rendered by MobileNavLayout based on the URL.
 * This page just exists to enable the /profile route.
 */
export default function ProfilePage() {
    return null;
}

